; Fill n bytes in memory.
;
; USE:  A X

!ifdef DEF_FILL_U8_BYTES !eof
DEF_FILL_U8_BYTES = 1

!macro fill_u8_bytes @dest_addr, @count, @value {
        ldx #@count
        lda #@value
-
        sta @dest_addr-1,x
        dex
        bne -
}
